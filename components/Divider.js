import { View } from "react-native";

export default function Divider({ image, title, description }) {
  return (
    <View className="w-full h-0.5 mt-3 opacity-20 drop-shadow-lg bg-[#20463C]">
    </View>
  );
}
